import SwiftUI

struct Sidebar: View {
    var body: some View {
        List {
            // Sidebar reserved for future use (spaces, collections)
        }
        .listStyle(.sidebar)
        .navigationTitle("Canopy")
    }
}
